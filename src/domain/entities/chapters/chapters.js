class Chapter {
    constructor(number, title, content, story) {
      this.number = number;
      this.title = title;
      this.content = content;
      this.story = story; // Referencia a la historia a la que pertenece el capítulo
    }
  
    // Métodos y lógica específica de la entidad Capitulo
  }
  
  module.exports = Chapter;
  